#include <iostream>

/*
    input: enter the password is a string
    process:
        - check the password have lenght is at least 6
        - check the password contain at least 1 digits
        - check the password contains at least one lowercase English character
        - check the password contains at least one uppercase
        - check the password contains at least one specical character

    output 
*/

int main() {

    return 0;
}